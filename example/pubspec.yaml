name: open_app_file_example
description: Demonstrates how to use the open_app_file plugin.

version: 4.0.2

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  path_provider: ^2.1.5
  http: ^1.4.0
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

  open_app_file:
    path: ../

flutter:
  uses-material-design: true
